// tailwind.config.js
export default {
  theme: {
    extend: {
      fontFamily: {
        affair: ['var(--font-affair)'],
        parismatchbright: ['var(--font-parismatch)'],
      },
    },
  },
}